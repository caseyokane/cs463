#include "hillClimb.h"

//determine the fit (or number of correct clauses)

//determine the neighbors for a given node by changing evaluations, find the 
//highest fit. Find this by determining number of correct clauses for every
//possible evaluation for that given formula (change one var at a time) 

int hillClimb(vector<vector<int>> v_currForm){
    //set the current node to that of the initial state 
   
    //enter primary loop of the algorithm
        //Set the neighbor node to the successor with hightest fit 
        //if neighbor.fit < = current.fit
            //return current.formula
        //current = neighbor if value greater

 
    return 0;
}
